    "invoked": "<%= lifecycle %>",
    "results": [
        <% last = results.pop %>
        <% results.each do |result| %>
        {
            "result": "<%= result %>"
        },
        <% end %>
        {
            "result": "<%= last %>"
        }
    ]
