    "invoked": "<%= lifecycle %>",
    "results": [
        <% last = results.pop %>
        <% results.each do |result| %>
        {
            "result": { <% result.each do |key, val| %>"<%= key %>": "<%= val %>" <% end %>}
        },
        <% end %>
        {
            "result": { <% last.each do |key, val| %>"<%= key %>": "<%= val %>" <% end %>}
        }
    ]
